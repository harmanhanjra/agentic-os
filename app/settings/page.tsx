import { WorkspacePage } from '@/components/workspace-page';
export default function SettingsPage() { return <WorkspacePage kind="settings" title="Settings" eyebrow="Control plane" description="Manage providers, encrypted credentials, model sync, routing preferences, usage budgets, privacy, and appearance." action="Add provider"/>; }
