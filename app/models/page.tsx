import { WorkspacePage } from '@/components/workspace-page';
export default function ModelsPage() { return <WorkspacePage kind="models" title="Model Registry" eyebrow="Capability control" description="Registered models are discovered from providers and filtered by capabilities, availability, context, local status, and your workspace preferences." action="Sync models"/>; }
