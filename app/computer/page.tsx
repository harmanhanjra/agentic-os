import { AppShell } from '@/components/app-shell';
import { ComputerClient } from '@/components/computer-client';

export default function ComputerPage() {
  return (
    <AppShell>
      <ComputerClient />
    </AppShell>
  );
}
