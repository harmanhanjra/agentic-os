import { WorkspacePage } from '@/components/workspace-page';
export default function ArenaPage() { return <WorkspacePage kind="arena" title="Model Arena" eyebrow="Compare outputs" description="Run one prompt across two to four registered models concurrently, then inspect latency, usage, provider, and cost metadata side by side." action="Create comparison"/>; }
